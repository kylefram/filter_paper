\chapter{Evaluation}

We evaluate our simulated camera stacks by comparing the reconstructed XYZ color matching functions from our selected filters to the target XYZ functions, summarized by the MSE loss. 
We also consider the color difference $\Delta E_{00}$ for a set of reflectances.
This latter method effectively approximates the human eye's response to color difference, and is therefore well suited for a practical evaluation of our reconstruction \cite{farnand2003using}. 
For the latter evaluation, we use the Macbeth Color Checker \cite{Pascale2006RGBCO} as our test set, and render XYZs and our RGBs  (found from our method to best approximate XYZs) under a D65 illuminant. 

Then, we test the prescribed filters on our physical camera, deployed in a controlled lighting laboratory setting. 

\section{MyColor Filter Set}

First, we review the results of the simulated camera on a set of 494 filters drawn from the Rosco MyColor tool. 
This large set of filters allows us to emulate our desired target function effectively, given the flexibility of our options. 
There is a subtle drawback, which is that we cannot effectively benchmark our filter selection process past $k=3$ due to the computational complexity of selecting $k>3$ filters, with $2.5 \times 10^9$ combinations for the $k=4$ case. 
However, we notice that even with just $k=3$ filters in use from the MyColor dataset, we are able to effectively attain $\Delta E_{00}$ of 1.07, already nearly 'just noticeable difference' \cite{lindstrom2008delta} across the mean of the 24 color patches.

\begin{figure}[H]
\centering
\begin{subfigure}{.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/scraped_k3_nDeltaEChart.png}
    \caption{Best $\Delta E_{00}$ for each number of scraped filters $k < 4$}
\end{subfigure}%
\begin{subfigure}{.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/scraped_k3_front_page_right.png}
    \caption{Transmission functions for optimal MyColor $k=3$ solution}
\end{subfigure}
\caption{MyColor $k=3$ performance and selected filters.}
\end{figure}

Indeed, we can visually inspect the transmission functions and the $\boldsymbol{M}$ matrix to see that our functions share approximate modes with the XYZ functions, given that the right tails will be neutralized above 650nm by the presence of our NOIR filter. 
%%TODO - INCLUDE M MATRIX

Their transformations after multiplication by $\boldsymbol{M}$ are shown below, including a simulated NOIR filter which cuts off most transmission above 650nm. 
These are overlaid 

\begin{figure}[H]
\centering
\begin{subfigure}{.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/scraped_k3_spectra.png}
    \caption{Transformed transmission functions with CIE 2006 XYZ spectral response functions}
\end{subfigure}%
\begin{subfigure}{.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/scraped_gamut_k3.png}
    \caption{Best filters from MyColor at $k=3$ on the color gamut}
\end{subfigure}
\caption{MyColor $k=3$ filters in spectral and chromaticity space.}
\end{figure}

Additionally, we can view the filters' dominant wavelength overlaid onto the CIE 1931 Chromacity Diagram in CIE x and CIE y space to see that these filters 'span' the entire gamut, and hence a linear combination of these filters may effectively reproduce any color on the gamut.
While we primarily utilize the New CIE 2006 LMS functions and their respective XYZ transformations (denoted CIE 2006), a significiant body of research exists referring to the 1931 XYZ functions which provide helpful benchmarks for our performance. 
Further, the CIE 1931 Color Gamut diagram provides a natural and familiar demonstration of color within the CIE x and CIE y coordinates.  

More rigorous analysis requires the use of $\Delta E_{00}$ calculations using a color checker, and in this case, our mean $\Delta E_{00}$ across 24 patches reached the value of 1.07.

\begin{figure}[H]
\centering
\begin{subfigure}{.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/scraped_k3_macbeth.png}
    \caption{Macbeth Color Checker for $k=3$ with MyColor filters}
\end{subfigure}%
\begin{subfigure}{.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/scraped_k1scaled_deltaE.png}
    \caption{$\Delta E_{00}$ for $k \in \{3...8\}$ and $k_1 \in \{0,1,2\}$}
\end{subfigure}
\caption{MyColor evaluation on a color checker and scaling to larger $k$.}
\end{figure}

While $\Delta E_{00}$ of 1.07 is sufficient for human-centered applications in controlled lighting, we can improve upon this result and strive for lower MSE results.
With the full set of 494 filters, we are unable to compute a brute-force solution in reasonable time, and must utilize heuristic solutions.
Fortunately, the heuristic solutions augmented with a few brute force rounds provide effective solutions. 
We show up to $k=8$ filters' $\Delta E_{00}$ progressions as a function of $k$ below. 

This result supports our intuition that we can achieve $\Delta E_{00}$ values comprable with state of the art results using our analysis.
Importantly, the improvement in $\Delta E_{00}$ largely flattens after $k=5$, providing useful knowledge as to the necessary numbner of filters to attain a high-quality reconstruction.
We include a rendering from the CAVE dataset reconstructed with the $k_1=4,k_2=1$ solution for an example of a practical simulated filter wheel setup. 

\begin{figure}[H]
\centering
\begin{subfigure}{.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/scraped_cave.png}
    \caption{CAVE rendering using best $k_1=4,k_2=1$ solution}
\end{subfigure}%
\begin{subfigure}{.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/deltaE_vs_k_mse.png}
    \caption{Best $\Delta E_{00}$ for each number of filters $k$ (Supergel set)}
\end{subfigure}
\caption{Example simulated reconstruction and overall Supergel performance.}
\end{figure}

\section{Rosco Supergel Set}

\subsection{MSE and $\Delta E_{00}$ Results}

For the 

In Figure 6, we show the best $\Delta E_{00}$ of our approximation at each $k$ filter subset of the Rosco Supergel set, where $k\in [1,8]$. For completeness, we use brute force search. 
%For each number of filters $N$, we compute the minimum $\Delta E_{00}$ across all combinations of $N$ filters from the Rosco Supergel set, and plot the relationship between $N$ and $\Delta E_{00}$.

We also plot the resultant reconstructed XYZ functions for the best $k-$filter combination of $k\in\{3,4,5,6\}$, see respectively Figures 7, 9, 10 and 12. 
%In the $k=3$ case, we can see visible artifacts in the transmission functions. 

%%% GRAHAM GOT TO HERE 19/4/26

\begin{figure}[H]
\centering
\begin{subfigure}{.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/k3top1_spectra.png}
    \caption{Camera spectral sensitivity for best XYZ reconstruction ($k=3$)}
\end{subfigure}%
\begin{subfigure}{.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/n3MSEMacbeth.png}
    \caption{Macbeth Color Checker comparison ($k=3$)}
\end{subfigure}
\caption{Supergel results for $k=3$.}
\end{figure}

The best filter combination for $k=3$, clearly a poor spectral fit, see Fig. 7, achieves a tolerable mean $\Delta E_{00}$ of 3.77. This is in the just noticeable difference threshold of 2-4 \cite{lindstrom2008delta},
indicating that the reconstructed colors may be distinguishable from the target colors under certain conditions.
The 24 patches below show the original in the top left of each pane and the simulated reconstruction in the bottom left. It is evident there is a visible colour difference.
%\begin{center}
%    \centering
%    \includegraphics[width=.5\linewidth]{media/n3MSEbarRank2.png}
%    \captionof{figure}{Best $\Delta E_{00}$ for each number of filters $N=3$ }
%\end{center}

We see a noticeable improvement in MSE and in $\Delta E_{00}$ with $k=4$, the latter measuring 1.09, slightly above the 'just noticeable difference' of 1.0 \cite{lindstrom2008delta}. 

\begin{figure}[H]
\centering
\begin{subfigure}{.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/k4top1_spectra.png}
    \caption{Camera spectral sensitivity for best XYZ reconstruction ($k=4$)}
\end{subfigure}%
\begin{subfigure}{.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/k5top1_spectra.png}
    \caption{Camera spectral sensitivity for best XYZ reconstruction ($k=5$)}
\end{subfigure}
\caption{Supergel spectral sensitivities for $k=4$ and $k=5$.}
\end{figure}

Next, $k=5$ achieves a $\Delta E_{00}$ of 0.35, which is well below the just noticeable difference threshold, indicating that the reconstructed colors are likely indistinguishable from the target colors under typical viewing conditions.
The following Macbeth color patches display nearly identical hues between target and simulated patches. 

\begin{figure}[H]
\centering
\begin{subfigure}{.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/k6top1_spectra.png}
    \caption{Camera spectral sensitivity for best XYZ reconstruction ($k=6$)}
\end{subfigure}
\begin{subfigure}{.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/k5Macbeth_render_rank_0.png}
    \caption{Macbeth Color Checker comparison ($k=5$)}
\end{subfigure}%

\caption{Supergel results for $k=5$ and $k=6$.}
\end{figure}

%The distribution of $\Delta E_{00}$ across the 24 Macbeth Color Checker patches for the best filter combination for $k=10$ shows that all patches achieve a $\Delta E_{00}$ below 0.1.

%\begin{center}
%    \centering
%    \includegraphics[width=.5\linewidth]{media/n10MSEbarRank0.png}
%    \captionof{figure}{Best $\Delta E_{00}$ for each number of filters $N$ }
%\end{center}

%In addition, the $3 \times 10$ $\boldsymbol{M}$ matrix for the best $k=10$ reconstruction is shown below.
%We note that all 10 filters contribute to at least two of the trichromatic values.
%\begin{center}
%    \centering
%    \includegraphics[width=.5\linewidth]{media/n10M.png}
%    \captionof{figure}{$\boldsymbol{M}$ matrix for $k=10$ reconstruction}
%\end{center}

Finally, $k=6$ achieves a $\Delta E_{00}$ of 0.39, and there is generally limited further decrease in $\Delta E_{00}$ at $k>6$. Notice here the spectral fit the XYZ CMFs is good.

\section{CAVE Dataset Visualization}

We are curious to see whether 

\subsection{Noise Model Analysis}

\begin{center}
    \centering
    \includegraphics[width=.75\linewidth]{media/photons_sweep_deltaE_20260528_210811.png}
    \captionof{figure}{Best $\Delta E_{00}$ as a function of Photons}
\end{center}

\section{Physical Camera with Rosco Supergel Set}

For the initial evaluation of our physical camera stack, we test the recommended best performing filter combinations against a multispectral scene including a PMC in a lab environment, using the Rosco Supergel set of filters. 
A few captures featured artifacts, and a number of filters preserved transmission fairly uniformly across $\omega$ but included 'frost' or 'blur' effects which occasionally performed well in simulations but often added poor elements to reconstructions.

\begin{figure}[H]
\centering
\begin{subfigure}{.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/Lab00.jpg}
    \caption{Filter 00 in RaspberryPi Camera, including artifacts in bottom right}
\end{subfigure}
\begin{subfigure}{.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/Lab104.jpg}
    \caption{Filter 104 in RaspberryPi Camera, excluded for blur effect}
\end{subfigure}
\begin{subfigure}{.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/Lab47.jpg}
    \caption{Filter 47 in RaspberryPi Camera}
\end{subfigure}
\begin{subfigure}{.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/Lab336.jpg}
    \caption{Filter 336 in RaspberryPi Camera}
\end{subfigure}
\caption{Monochrome Captures in Lab Environment}
\end{figure}



\begin{center}
    \centering
    \includegraphics[width=.75\linewidth]{media/k1_4_recon.png}
    \captionof{figure}{$k_1=4$ Photo Lab Reconstruction with component captures above}
\end{center}



\begin{center}
    \centering
    \includegraphics[width=.75\linewidth]{media/k1_5_recon.png}
    \captionof{figure}{$k_1=5$ Photo Lab Reconstruction with component captures above}
\end{center}

For demonstration, we show the reconstruction for the $k=5$ case, where we simulate the optimal reconstruction including an IR-cut camera. 
However, the lab measurements were taken in the absence of an IR-cut camera, resulting in an ill-fitting reconstruction.

\begin{center}
    \centering
    \includegraphics[width=.75\linewidth]{media/k1_5_recon_badNOIR.png}
    \captionof{figure}{$k_1=5$ Photo Lab Reconstruction with component captures above, incorrect camera configuration}
\end{center}

Another poor example has another $k=5$ solution, but with the poor captures included. 


\begin{figure}[H]
\centering
\begin{subfigure}{.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/k5_noisy_render.png}
    \caption{Poor quality $k=5$ reconstruction including unfit filters 00, 100, 160}
\end{subfigure}
\begin{subfigure}{.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{media/k5_noisy_spectra.png}
    \caption{Spectral reconstruction for $k=5$ combination with low MSE, poor realized result}
\end{subfigure}
\caption{$k=5$ filter combination with poor reconstruction quality}
\end{figure}